<form method="post" action="{{ path('app_empty_jar_stat_crud_delete', {'id': empty_jar_stat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ empty_jar_stat.id) }}">
    <button class="btn">Delete</button>
</form>
