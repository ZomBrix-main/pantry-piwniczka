{% extends 'base.html.twig' %}

{% block title %}EmptyJarStat{% endblock %}

{% block body %}
    <h1>EmptyJarStat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ empty_jar_stat.id }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ empty_jar_stat.type }}</td>
            </tr>
            <tr>
                <th>Quantity</th>
                <td>{{ empty_jar_stat.quantity }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_empty_jar_stat_crud_index') }}">back to list</a>

    <a href="{{ path('app_empty_jar_stat_crud_edit', {'id': empty_jar_stat.id}) }}">edit</a>

    {{ include('empty_jar_stat_crud/_delete_form.html.twig') }}
{% endblock %}
